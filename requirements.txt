# Entorno: Python 3.11 o 3.12 (PyTorch aun no publica ruedas estables para 3.14).
# En Windows sin GPU, pip instala la rueda de CPU por defecto. En Colab/Kaggle torch ya viene.
#
# Conjunto reducido de dependencias. scipy se usa para una sola llamada (ndimage.label, la
# region conectada mas grande de la mascara) y ya viene como dependencia de scikit-learn.

torch>=2.2
torchvision>=0.17           # modelos preentrenados y transforms.v2 (imagen + mascara)

# Datos
numpy>=1.26
pandas>=2.1
pillow>=10.2
scipy>=1.11

# Metricas y utilidades
scikit-learn>=1.4
pyyaml>=6.0
matplotlib>=3.8

# Optimizacion (§4.4)
onnx>=1.16
onnxruntime>=1.17
onnxscript>=0.2          # exportador ONNX de las versiones recientes de PyTorch

# Despliegue (§4.6)
streamlit>=1.50
